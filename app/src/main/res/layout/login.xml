<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditText
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:id="@+id/et_loginEmail"
        android:hint="Email"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.3"/>

    <EditText
        android:id="@+id/et_loginPwd"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:hint="Password"
        android:inputType="textPassword"
        android:layout_marginTop="20dp"
        app:layout_constraintEnd_toEndOf="@+id/et_loginEmail"
        app:layout_constraintStart_toStartOf="@+id/et_loginEmail"
        app:layout_constraintTop_toBottomOf="@id/et_loginEmail"
        app:layout_constraintBottom_toTopOf="@id/btn_login"/>

    <Button
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/btn_login"
        android:text="로그인"
        android:layout_marginTop="30dp"
        android:layout_marginRight="7dp"
        app:layout_constraintTop_toBottomOf="@id/et_loginPwd"
        app:layout_constraintLeft_toLeftOf="@id/et_loginPwd"
        app:layout_constraintRight_toLeftOf="@id/btn_toRegister"
        android:background="@color/lightBlue"/>

    <Button
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/btn_toRegister"
        android:text="회원가입"
        android:layout_marginTop="30dp"
        android:layout_marginLeft="7dp"
        app:layout_constraintTop_toBottomOf="@id/et_loginPwd"
        app:layout_constraintLeft_toRightOf="@id/btn_login"
        app:layout_constraintRight_toRightOf="@id/et_loginPwd"
        android:background="@color/lightBlue"/>



</androidx.constraintlayout.widget.ConstraintLayout>